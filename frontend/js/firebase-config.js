// Placeholder — run `npm run config` (reads .env) to generate the real file. See README.
window.WIR_CONFIG = {
  firebase: { apiKey: '', authDomain: '', projectId: '', appId: '', messagingSenderId: '' },
  apiBaseUrl: 'http://localhost:5000/api'
};
